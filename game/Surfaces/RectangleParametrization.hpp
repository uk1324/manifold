#pragma once

//6template<typename T>

#include "ChristoffelSymbols.hpp"
#include "Connectivity.hpp"
#include <engine/Math/Vec3.hpp>

//template<typename Parametrization>
//concept RectangleParametrization = requires(Parametrization surface, f32 u, f32 v) {
//	{ surfaceNormal(surface, u, v) } -> Vec3;
//	//{ scalar* stateVector } -> std::convertible_to<StateVector>;
//	//{ stateVector + stateVector } -> std::convertible_to<StateVector>;
//};
